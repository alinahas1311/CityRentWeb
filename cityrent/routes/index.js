var express = require('express');
var router = express.Router();

// const db = require('../public/db');

/* ============================================================================= ===================== ============================================================================= */
/* ============================================================================= ===================== ============================================================================= */
/* ============================================================================= ROOTS ---- FETCH DATA ============================================================================= */
/* ============================================================================= ===================== ============================================================================= */
/* ============================================================================= ===================== ============================================================================= */

router.get('/ourservice', function(req, res, next) {
  res.render('ourservice', { title: 'Services' });
});

//////////////////// GET home page
router.get('/', function(req, res, next) {
  res.render('index', { title: 'City Rent' });
});


router.get('/fleet', function(req, res, next) {
  res.render('fleet', { title: 'City Rent' });
});




//////////////////// GET artcles page
router.get('/article/:department?', function(req, res, next) {
  const department = req.params.department;

  res.render('articlesPage', { title: 'City Rent', department: department});
});

//////////////////// GET produit page




module.exports = router;
